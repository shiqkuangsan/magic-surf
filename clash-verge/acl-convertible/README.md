# ACL Convertible

This directory is documentation-only.

For subscriptions that can use ACL conversion safely:
- use ACL conversion for main groups and routing
- keep AI routing unified to `💬 Ai平台`
- avoid storing generated conversion output in this repository
- avoid separate Clash Verge `rules/groups/proxies` overlays unless absolutely necessary

Use `shared/Merge.yaml` only for common patches that should apply to every subscription.

